# ── Python ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# ── Environment / Secrets ──
.env
*.env
credentials.json

# ── Simulation outputs (large, per-run) ──
simulation_results/
archives/
results_*.tar.gz
results_*.tar.gz.part_*
results_*.tar.gz.sha256
random_calibration_*.tar.gz
*.truncated.tar.gz

# Temporary diagnostics / extracted research text
validate_regressive_tax_pipeline.py
*_text.txt

# ── Research documents (not for public) ──
thesis/
*.pdf
*.docx
*.doc

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Claude Code internal (CLAUDE.md, memory, etc.) ──
.claude/
CLAUDE.md
AGENTS.md

# ── Virtual environment ──
venv/
.venv/

# ── OS ──
Thumbs.db
.DS_Store
